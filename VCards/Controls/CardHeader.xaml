<UserControl x:Class="VCards.Controls.CardHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VCards.Controls"
             xmlns:mat="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="32" MaxHeight="32" d:DesignWidth="300">
    <Grid Background="{DynamicResource PrimaryHueDarkBrush}">
        <Grid VerticalAlignment="Bottom">

            <TextBox Name="TitleBox" Foreground="White" Background="{DynamicResource PrimaryHueDarkBrush}" mat:HintAssist.Hint="Title" HorizontalAlignment="Left" MinWidth="32" Margin="5,0,0,0"/>

            <Button HorizontalAlignment="Right" Background="Transparent" BorderBrush="Transparent" Name="Close">
                <mat:PackIcon Kind="Delete"/>
            </Button>
        </Grid>
    </Grid>
</UserControl>
